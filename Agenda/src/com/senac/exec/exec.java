package com.senac.exec;

import com.senac.estrutura.lista.ListaOrdenada;
import com.senac.estrutura.lista.Nodo;
import com.senac.model.FileManager;
import com.senac.app.*;

public class exec {

	public static void main(String[] args) {
		
		Tela console = new Tela();
		console.menuPrincipal();
		
        //FileManager t = new FileManager();  
        //t.Read();
        //System.out.println(contatos[0]);
        
        
		/*
		// criar lista
		ListaOrdenada<Integer> lista = new ListaOrdenada<Integer>();

		Nodo<Integer> novo = new Nodo<Integer>(1);
		lista.insert(novo);

		lista.insert(new Nodo<Integer>(2), novo);

		lista.append(new Nodo<Integer>(3));

		lista.insert(new Nodo<Integer>(4), novo);

		lista.print();
		*/
	}

}
